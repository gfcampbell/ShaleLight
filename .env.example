# PostgreSQL (Docker)
DATABASE_URL=postgresql://shalelight:shalelight_local@localhost:5432/shalelight

# Auth
JWT_SECRET=change-this-to-a-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-too

# Critical: use Ollama for local/air-gapped operation.
# OpenAI/Anthropic require outbound network access and send model traffic externally.
# Options: ollama, openai, anthropic
AI_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434

# Optional: External provider API keys (only needed if switching provider in admin)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# File browsing security (comma-separated allowed root paths)
ALLOWED_FILE_ROOTS=/Volumes,/data

# Optional
NODE_ENV=development
PORT=3000
